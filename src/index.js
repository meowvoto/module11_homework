export function getPercents(percent, number) {
	return number / 100 * percent;
}

// Тренажер по JavaScript -> Функции -> Задание 2.
// Напишите функцию getPercents(percent, number), которая возвращает {percent} процентов от {number}.